init_config:

instances:
  - directory: "/var/checks/check_ip"
    name: check_nat_ip
    tags:
      - directory:check_ip

  - directory: "/var/checks/still_alive"
    name: check_nat_ip_alive
    tags:
      - directory:still_alive